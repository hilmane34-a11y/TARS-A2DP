name: Build TARS A2DP V2 Verified

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git \
            make \
            gcc \
            g++ \
            cmake \
            ninja-build \
            python3 \
            python3-pip \
            flex \
            bison \
            gperf \
            ccache \
            libffi-dev \
            libssl-dev \
            dfu-util \
            libusb-1.0-0

      - name: Verify repository structure
        run: |
          echo "===== MODULE FILES ====="
          find modules -type f -print

          echo ""
          echo "===== modules/micropython.cmake ====="
          cat modules/micropython.cmake

          echo ""
          echo "===== modules/tars_a2dp/micropython.cmake ====="
          cat modules/tars_a2dp/micropython.cmake

          echo ""
          echo "===== MODULE REGISTRATION ====="
          grep -n "MP_REGISTER_MODULE" modules/tars_a2dp/tars_a2dp.c

      - name: Clone MicroPython
        run: |
          git clone --depth 1 \
            https://github.com/micropython/micropython.git \
            micropython

      - name: Setup MicroPython submodules
        run: |
          cd micropython
          git submodule update \
            --init \
            --recursive \
            --depth 1

      - name: Clone ESP-IDF
        run: |
          git clone \
            --depth 1 \
            --branch v5.5.2 \
            https://github.com/espressif/esp-idf.git \
            esp-idf

          cd esp-idf

          git submodule update \
            --init \
            --recursive \
            --filter=tree:0

      - name: Install ESP-IDF
        run: |
          cd esp-idf
          ./install.sh esp32

      - name: Build mpy-cross
        run: |
          cd micropython
          make -C mpy-cross

      - name: Setup ESP32 submodules
        shell: bash
        run: |
          . "$GITHUB_WORKSPACE/esp-idf/export.sh"

          cd "$GITHUB_WORKSPACE/micropython"

          make -C ports/esp32 submodules

      - name: Clean previous build
        run: |
          rm -rf micropython/ports/esp32/build-ESP32_GENERIC

      - name: Build TARS MicroPython
        shell: bash
        run: |
          . "$GITHUB_WORKSPACE/esp-idf/export.sh"

          cd "$GITHUB_WORKSPACE/micropython"

          make -C ports/esp32 \
            BOARD=ESP32_GENERIC \
            USER_C_MODULES="$GITHUB_WORKSPACE/modules/micropython.cmake"

      - name: Verify firmware output
        run: |
          FIRMWARE="micropython/ports/esp32/build-ESP32_GENERIC/firmware.bin"
          ELF="micropython/ports/esp32/build-ESP32_GENERIC/micropython.elf"

          echo "===== CHECK FIRMWARE ====="
          test -f "$FIRMWARE"
          ls -lh "$FIRMWARE"

          echo ""
          echo "===== CHECK ELF ====="
          test -f "$ELF"
          ls -lh "$ELF"

      - name: Verify TARS A2DP symbols
        shell: bash
        run: |
          ELF="micropython/ports/esp32/build-ESP32_GENERIC/micropython.elf"

          echo "===== SEARCHING FOR TARS A2DP SYMBOLS ====="

          TOOL=$(find "$HOME/.espressif/tools" \
            -type f \
            -name xtensa-esp32-elf-nm \
            | head -n 1)

          if [ -z "$TOOL" ]; then
            echo "ERROR: xtensa-esp32-elf-nm not found"
            exit 1
          fi

          echo "Using tool: $TOOL"

          echo ""
          echo "===== TARS A2DP SYMBOLS ====="

          "$TOOL" "$ELF" | grep "tars_a2dp" || {
            echo "ERROR: No tars_a2dp symbol found in ELF"
            exit 1
          }

          echo ""
          echo "SUCCESS: TARS A2DP symbols found"

      - name: Verify TARS A2DP object file
        run: |
          BUILD="micropython/ports/esp32/build-ESP32_GENERIC"

          echo "===== SEARCHING OBJECT FILE ====="

          find "$BUILD" \
            -type f \
            -name "*tars_a2dp*.obj" \
            -print

          COUNT=$(find "$BUILD" \
            -type f \
            -name "*tars_a2dp*.obj" \
            | wc -l)

          echo "Object files found: $COUNT"

          if [ "$COUNT" -eq 0 ]; then
            echo "ERROR: tars_a2dp.c was not compiled"
            exit 1
          fi

      - name: Inspect module strings
        shell: bash
        run: |
          ELF="micropython/ports/esp32/build-ESP32_GENERIC/micropython.elf"

          TOOL=$(find "$HOME/.espressif/tools" \
            -type f \
            -name xtensa-esp32-elf-strings \
            | head -n 1)

          if [ -z "$TOOL" ]; then
            echo "WARNING: xtensa-esp32-elf-strings not found"
            exit 0
          fi

          echo "===== TARS MODULE STRINGS ====="

          "$TOOL" "$ELF" \
            | grep -E "tars_a2dp|TARS A2DP" \
            || echo "No readable TARS A2DP string found"

      - name: Prepare firmware
        run: |
          mkdir -p output

          cp \
            micropython/ports/esp32/build-ESP32_GENERIC/firmware.bin \
            output/TARS-MicroPython-ESP32-A2DP-V2.bin

          echo "===== FINAL OUTPUT ====="
          ls -lh output/

      - name: Create build information
        shell: bash
        run: |
          {
            echo "TARS MicroPython A2DP V2"
            echo ""
            echo "Build date:"
            date -u
            echo ""
            echo "Commit:"
            git rev-parse HEAD
            echo ""
            echo "Module registration:"
            grep "MP_REGISTER_MODULE" \
              modules/tars_a2dp/tars_a2dp.c
          } > output/BUILD-INFO.txt

          cat output/BUILD-INFO.txt

      - name: Upload verified firmware
        uses: actions/upload-artifact@v4
        with:
          name: TARS-MicroPython-ESP32-A2DP-V2
          path: |
            output/TARS-MicroPython-ESP32-A2DP-V2.bin
            output/BUILD-INFO.txt
